import { Box } from '@mui/material'
import { useNavigate } from 'react-router-dom'

const Profile = () => {
  const navigate = useNavigate()

  // () => navigate('/')

  return <Box>My profile HELLO</Box>
}
export default Profile
